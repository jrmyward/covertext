# Copy this file to .kamal/secrets and fill in actual values
# DO NOT commit .kamal/secrets to git!

# GitHub Container Registry Personal Access Token
# Create at: GitHub Settings → Developer settings → Personal access tokens
# Required scope: write:packages
KAMAL_REGISTRY_PASSWORD=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Rails master key (from config/master.key)
RAILS_MASTER_KEY=$(cat config/master.key)

# Twilio credentials (optional if using Rails encrypted credentials)
# If you prefer ENV vars over Rails credentials, uncomment these:
# TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# TWILIO_AUTH_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Postgres password (generate a strong random password)
# Example: openssl rand -base64 32
POSTGRES_PASSWORD=your_strong_random_password_here
