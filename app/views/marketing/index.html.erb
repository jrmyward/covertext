<% content_for :title, "CoverText - Automated Insurance Client Service via SMS" %>

<!-- Hero Section -->
  <div class="hero min-h-screen bg-gradient-to-br from-primary/10 via-base-100 to-secondary/10">
    <div class="hero-content text-center max-w-4xl px-4">
      <div>
        <!-- Logo/Brand -->
        <h1 class="text-5xl font-bold mb-6 text-primary">CoverText</h1>

        <!-- Headline -->
        <h2 class="text-4xl md:text-5xl font-bold mb-6 leading-tight">
          Let Clients Text for Insurance Cards — Not Call Your Staff
        </h2>

        <!-- Subhead -->
        <p class="text-xl md:text-2xl mb-8 text-base-content/80 leading-relaxed max-w-3xl mx-auto">
          CoverText automates routine insurance service requests like ID cards and policy expirations through secure, compliant SMS.
        </p>

        <!-- CTAs -->
        <div class="flex flex-col sm:flex-row gap-4 justify-center mb-12">
          <%= link_to "Get Started", signup_path, class: "btn btn-primary btn-lg" %>
          <a href="mailto:hello@covertext.io" class="btn btn-outline btn-lg">Request a Demo</a>
        </div>
      </div>
    </div>
  </div>

  <!-- What CoverText Does -->

  <%= render UI::SectionComponent.new(bg: :light) do %>
    <h3 class="text-3xl font-bold text-center mb-12">What CoverText Does</h3>
    <div class="grid md:grid-cols-3 gap-8">
      <%= render UI::CardComponent.new do %>
        <div class="text-4xl mb-4">📱</div>
        <h4 class="card-title">Clients Text Your Number</h4>
        <p>Your agency gets a dedicated SMS number. Clients text it just like they'd call.</p>
      <% end %>
      <%= render UI::CardComponent.new do %>
        <div class="text-4xl mb-4">⚡</div>
        <h4 class="card-title">Automatic Fulfillment</h4>
        <p>CoverText identifies and fulfills common requests instantly, no manual work required.</p>
      <% end %>
      <%= render UI::CardComponent.new do %>
        <div class="text-4xl mb-4">📋</div>
        <h4 class="card-title">Full Audit Trail</h4>
        <p>Every request is logged and auditable. You stay in control of your client data.</p>
      <% end %>
    </div>
  <% end %>

  <!-- What It Handles -->
  <%= render UI::SectionComponent.new do %>
    <h3 class="text-3xl font-bold text-center mb-12">What It Handles</h3>
    <div class="space-y-4">
      <%= render UI::FeatureItemComponent.new(
        icon: "✅",
        title: "Insurance ID Cards",
        description: "Clients receive their policy documents via MMS instantly"
      ) %>
      <%= render UI::FeatureItemComponent.new(
        icon: "✅",
        title: "Policy Expiration Lookups",
        description: "Quick answers about when policies expire"
      ) %>
      <%= render UI::FeatureItemComponent.new(
        icon: "✅",
        title: "Graceful Fallback",
        description: "Unsupported requests get a helpful \"we'll call you\" response"
      ) %>
    </div>
  <% end %>

  <!-- What It Does NOT Do -->
  <%= render UI::SectionComponent.new(bg: :light) do %>
    <h3 class="text-3xl font-bold text-center mb-4">What CoverText Does NOT Do</h3>
    <p class="text-center text-base-content/70 mb-12">We're intentionally narrow to stay simple and compliant</p>
    <div class="grid md:grid-cols-2 gap-6">
      <%= render UI::FeatureItemComponent.new(icon: "❌", icon_size: "text-xl", gap: "gap-3") do %>
        <strong>No policy changes</strong> — those require agent approval
      <% end %>
      <%= render UI::FeatureItemComponent.new(icon: "❌", icon_size: "text-xl", gap: "gap-3") do %>
        <strong>No billing</strong> — payment processing stays with you
      <% end %>
      <%= render UI::FeatureItemComponent.new(icon: "❌", icon_size: "text-xl", gap: "gap-3") do %>
        <strong>No underwriting</strong> — quotes and risk assessment aren't automated
      <% end %>
      <%= render UI::FeatureItemComponent.new(icon: "❌", icon_size: "text-xl", gap: "gap-3") do %>
        <strong>No carrier integrations</strong> — we work with your existing data
      <% end %>
      <%= render UI::FeatureItemComponent.new(icon: "❌", icon_size: "text-xl", gap: "gap-3") do %>
        <strong>No client portal</strong> — SMS is the interface, period
      <% end %>
    </div>
  <% end %>

  <!-- How It Works -->
  <%= render UI::SectionComponent.new do %>
    <h3 class="text-3xl font-bold text-center mb-12">How It Works</h3>
    <div class="grid md:grid-cols-4 gap-8">
      <%= render UI::StepComponent.new(
        number: 1,
        title: "Client Texts",
        description: "They send a message to your agency number"
      ) %>
      <%= render UI::StepComponent.new(
        number: 2,
        title: "CoverText Identifies",
        description: "Intent is detected and the client is matched"
      ) %>
      <%= render UI::StepComponent.new(
        number: 3,
        title: "Instant Delivery",
        description: "Document or answer is sent automatically"
      ) %>
      <%= render UI::StepComponent.new(
        number: 4,
        title: "Staff Stays Focused",
        description: "Your team works on high-value tasks instead"
      ) %>
    </div>
  <% end %>

  <!-- Compliance & Trust -->
  <%= render UI::SectionComponent.new(bg: :light) do %>
    <h3 class="text-3xl font-bold text-center mb-12">Compliance & Trust</h3>
    <div class="grid md:grid-cols-2 gap-8">
      <%= render UI::FeatureItemComponent.new(
        icon: "🔒",
        title: "STOP / START / HELP Supported",
        description: "Full compliance with SMS regulations"
      ) %>
      <%= render UI::FeatureItemComponent.new(
        icon: "🚫",
        title: "Opt-Out Enforcement",
        description: "Clients can opt out anytime, automatically honored"
      ) %>
      <%= render UI::FeatureItemComponent.new(
        icon: "📊",
        title: "Full Audit Trail",
        description: "Every message and action is logged"
      ) %>
      <%= render UI::FeatureItemComponent.new(
        icon: "🏢",
        title: "You Own Your Data",
        description: "Your agency data stays yours, always"
      ) %>
    </div>
  <% end %>

  <!-- Pricing -->
  <%= render UI::SectionComponent.new(py: "py-8 lg:py-20", tag: :section, id: "pricing", inner_class: "group") do %>
    <div class="text-center">
      <h2 class="text-3xl font-semibold sm:text-4xl">Pricing Plans</h2>
      <p class="text-base-content/80 mt-2 sm:text-lg">
        Flexible pricing. No hidden fees. Scale with your needs.
      </p>
    </div>

    <div class="mt-8 flex justify-center">
      <div role="tablist" class="tabs tabs-box relative w-fit">
        <input
          type="radio"
          name="pricing-tab"
          role="tab"
          value="monthly"
          class="tab px-8"
          aria-label="Monthly">
        <input
          type="radio"
          name="pricing-tab"
          role="tab"
          class="tab px-8"
          value="yearly"
          aria-label="Yearly"
          checked>
        <span class="badge badge-neutral absolute -end-10 -top-2">-40%</span>
      </div>
    </div>

    <div class="mt-8 grid gap-8 lg:grid-cols-3">
      <%= render UI::CardComponent.new(outer_class: "card border-base-300 border p-3") do %>
        <div class="bg-base-200 p-6">
          <h3 class="text-xl font-semibold">Free</h3>
          <p class="mt-2 flex items-baseline">
            <span class="text-5xl font-semibold tracking-tight">$0</span>
            <span class="ml-1 text-xl font-medium">/month</span>
          </p>
          <p class="mt-4">
            Experience essential features at no cost, setting the foundation for
            your business growth.
          </p>
        </div>
        <div class="p-6">
          <ul class="list-inside list-disc space-y-3" role="list">
            <li>
              <span class="ms-3">For Personal Use</span>
            </li>
            <li>
              <span class="ms-3">20 products</span>
            </li>
            <li>
              <span class="ms-3">Limited Integrations</span>
            </li>
          </ul>
        </div>
        <button class="btn btn-outline border-base-300 btn-block mt-auto">
          Start for Free
        </button>
      <% end %>

      <%= render UI::CardComponent.new(outer_class: "card border-base-300 border p-3") do %>
        <div class="bg-primary/10 rounded-box p-6">
          <div class="flex justify-between">
            <h3 class="text-primary text-xl font-semibold">Premium</h3>
            <div class="badge badge-primary badge-sm">Most Popular</div>
          </div>

          <p class="mt-4 flex items-baseline">
            <span class="text-primary text-5xl font-semibold tracking-tight group-has-[[value=yearly]:checked]:hidden">
              $99
            </span>
            <span class="text-primary text-5xl font-semibold tracking-tight group-has-[[value=monthly]:checked]:hidden">
              $59
            </span>
            <span class="ml-1 text-xl font-medium">/month</span>
          </p>
          <p class="mt-6">
            Unlock advanced features and elevate your SaaS experience with our
            premium plan.
          </p>
        </div>
        <div class="p-6">
          <ul class="list-inside list-disc space-y-3" role="list">
            <li>
              <span class="ms-3">Up to 10 Team Members</span>
            </li>
            <li>
              <span class="ms-3">Unlocked all Apps</span>
            </li>
            <li>
              <span class="ms-3">Use all Integrations</span>
            </li>
            <li>
              <span class="ms-3">99.9% Support Response Time</span>
            </li>
          </ul>
        </div>
        <button class="btn btn-primary btn-block mt-auto">
          Upgrade to Premium
        </button>
      <% end %>

      <%= render UI::CardComponent.new(outer_class: "card border-base-300 border p-3") do %>
        <div class="bg-secondary/10 rounded-box p-6">
          <h3 class="text-secondary text-xl font-medium">Ultimate</h3>
          <p class="mt-4 flex items-baseline">
            <span class="text-secondary text-5xl font-semibold tracking-tight group-has-[[value=yearly]:checked]:hidden">
              $199
            </span>
            <span class="text-secondary text-5xl font-semibold tracking-tight group-has-[[value=monthly]:checked]:hidden">
              $159
            </span>
            <span class="ml-1 text-xl font-semibold">/month</span>
          </p>
          <p class="mt-6 line-clamp-2 overflow-ellipsis">
            Unleash business potential with our ultimate plan. Maximize
            features, scalability, and success.
          </p>
        </div>
        <div class="p-6">
          <ul class="list-inside list-disc space-y-3" role="list">
            <li>
              <span class="ms-3">Unlimited Members</span>
            </li>
            <li>
              <span class="ms-3">Unlocked all Apps</span>
            </li>
            <li>
              <span class="ms-3">Use all Integrations</span>
            </li>
            <li>
              <span class="ms-3">24*7 Quick Support</span>
            </li>
            <li>
              <span class="ms-3">Marketing automations</span>
            </li>
          </ul>
        </div>
        <button class="btn btn-secondary btn-block mt-auto">
          Get Ultimate Power
        </button>
      <% end %>
    </div>
  <% end %>

  <!-- Footer -->
  <footer class="footer footer-center p-10 bg-base-200 text-base-content">
    <div>
      <p class="font-bold text-lg">CoverText</p>
      <p>Automated insurance client service via SMS</p>
    </div>
    <div>
      <div class="grid grid-flow-col gap-4">
        <a href="mailto:hello@covertext.io" class="link link-hover">Contact</a>
        <a href="#" class="link link-hover">Privacy Policy</a>
        <a href="#" class="link link-hover">Terms of Service</a>
      </div>
    </div>
    <div>
      <p>© <%= Time.current.year %> CoverText. All rights reserved.</p>
    </div>
  </footer>
