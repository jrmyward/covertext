# Name of your application. Used to uniquely configure containers.
service: covertext

# Name of the container image (use your-user/app-name on external registries).
# Replace <GITHUB_OWNER> with your GitHub username or organization
image: ghcr.io/jrmyward/covertext

# Deploy to these servers.
# Use destinations to separate staging and production deployments
servers:
  web:
    hosts:
      - 1.2.3.4  # Will be overridden by destination configs

# Destination-specific configurations
destinations:
  staging:
    servers:
      web:
        hosts:
          - 159.65.70.168  # Staging droplet
    volumes:
      - "/var/lib/covertext/staging/db:/rails/db"
      - "/var/lib/covertext/staging/storage:/rails/storage"
    env:
      clear:
        RAILS_ENV: staging
        APP_HOST: staging.covertext.app
        APP_PROTOCOL: https
        SOLID_QUEUE_IN_PUMA: true
        # Postgres accessory runs on same server
        DATABASE_URL: postgres://postgres:password@covertext-postgres-staging:5432/covertext_staging
    proxy:
      ssl: true
      host: staging.covertext.app
    accessories:
      postgres:
        service: covertext-postgres-staging
        image: postgres:16
        host: 159.65.70.168  # Same as web server
        port: 127.0.0.1:5432:5432
        env:
          clear:
            POSTGRES_USER: postgres
            POSTGRES_DB: covertext_staging
          secret:
            - POSTGRES_PASSWORD
        directories:
          - data:/var/lib/postgresql/data

  production:
    servers:
      web:
        hosts:
          - 5.6.7.8  # Replace with production server IP
    volumes:
      - "/var/lib/covertext/production/db:/rails/db"
      - "/var/lib/covertext/production/storage:/rails/storage"
    env:
      clear:
        RAILS_ENV: production
        APP_HOST: covertext.app
        APP_PROTOCOL: https
        SOLID_QUEUE_IN_PUMA: true
        # External Digital Ocean managed Postgres
      secret:
        - DATABASE_URL  # Provided via .kamal/secrets
    proxy:
      ssl: true
      host: covertext.app

# Enable SSL auto certification via Let's Encrypt (HTTP → HTTPS redirect automatic)
# This is overridden by destination-specific proxy configs above
proxy:
  ssl: true
  host: staging.covertext.app

# Where you keep your container images.
registry:
  server: ghcr.io
  # Replace <GITHUB_USERNAME> with your GitHub username
  username: jrmyward
  password:
    - KAMAL_REGISTRY_PASSWORD

# Inject ENV variables into containers (secrets come from .kamal/secrets).
env:
  secret:
    - RAILS_MASTER_KEY
    # Twilio credentials (optional if using Rails encrypted credentials)
    # Uncomment if not using config/credentials/production.yml.enc
    # - TWILIO_ACCOUNT_SID
    # - TWILIO_AUTH_TOKEN
  clear:
    # Default environment variables (can be overridden by destinations)
    RAILS_ENV: production
    SOLID_QUEUE_IN_PUMA: true

# Aliases are triggered with "bin/kamal <alias>". You can overwrite arguments on invocation:
# "bin/kamal logs -r job" will tail logs from the first server in the job section.
aliases:
  console: app exec --interactive --reuse "bin/rails console"
  shell: app exec --interactive --reuse "bash"
  logs: app logs -f
  dbc: app exec --interactive --reuse "bin/rails dbconsole --include-password"

# Use a persistent storage volume for sqlite database files and local Active Storage files.
# Recommended to change this to a mounted volume path that is backed up off server.
volumes:
  - "covertext_storage:/rails/storage"

# Bridge fingerprinted assets, like JS and CSS, between versions to avoid
# hitting 404 on in-flight requests. Combines all files from new and old
# version inside the asset_path.
asset_path: /rails/public/assets

# Configure the image builder.
builder:
  arch: amd64

  # # Build image via remote server (useful for faster amd64 builds on arm64 computers)
  # remote: ssh://docker@docker-builder-server
  #
  # # Pass arguments and secrets to the Docker build process
  # args:
  #   RUBY_VERSION: ruby-3.3.5
  # secrets:
  #   - GITHUB_TOKEN
  #   - RAILS_MASTER_KEY

# Use a different ssh user than root
# ssh:
#   user: app

